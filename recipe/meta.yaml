{% set version = "3.5.1" %}

package:
  name: dictionaryutils
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/d/dictionaryutils/dictionaryutils-{{ version }}.tar.gz
  sha256: c63a47479f671881f0f00aa7f647751239f02cbc930536246a30d349bc1ea34b

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ={{ python_min }}
    - poetry
    - poetry-core
    - pip
  run:
    - python >={{ python_min }}
    - pyyaml
    - jsonschema >=2.5,<4
    - cdislogging
    - requests

test:
  commands:
    - pip check
  imports:
    - dictionaryutils
  requires:
    - pip
    - python ={{ python_min }}

about:
  home: https://github.com/uc-cdis/dictionaryutils
  summary: Python wrapper and metaschema for datadictionary.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - danielnachun
